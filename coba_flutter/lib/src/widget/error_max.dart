import 'package:flutter/material.dart';

class ErrorPro extends StatelessWidget {
  const ErrorPro({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('UNKNOWN ERROR'),
    );
  }
}
